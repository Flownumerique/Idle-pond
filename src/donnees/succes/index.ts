/**
 * IdlePond — registre des succès, un fichier par famille.
 *
 * VIDE À CE JALON. Les succès de l'assise I sont le contenu du jalon v0.2, avec
 * le plancher de cadence du §8.4 (premier succès sous deux minutes, un
 * déclenchement toutes les 3 à 5 minutes sur la première demi-heure, un
 * franchissement obligatoire à la première éclosion).
 *
 * Le TYPAGE par famille, l'état de VISIBILITÉ et le registre FIGÉ ne se
 * rétrofitent pas (§8) : ils sont posés ici, avant la première ligne de
 * contenu, et c'est la seule raison d'être de ce fichier à ce jalon.
 */
import type { Succes } from '../../noyau/types'

export const SUCCES: readonly Succes[] = []
